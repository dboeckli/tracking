# this yaml is merged into the values file and deleted before using helm
bitnamiKafkaImage:
  repository: bitnami/kafka
  tag: "4.0.0"
  pullPolicy: IfNotPresent

busyboxImage:
  repository: busybox
  tag: "1.37.0"
  pullPolicy: IfNotPresent